% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{weighted.mean}
\alias{weighted.mean}
\title{NA-aware weighted mean}
\usage{
weighted.mean(x, w, ..., na.rm = TRUE, all_na = NULL)
}
\arguments{
\item{x}{A numeric vector of values.}

\item{w}{A numeric vector of weights the same length as \code{x}.}

\item{...}{Additional arguments passed to \code{\link[stats:weighted.mean]{stats::weighted.mean()}}.}

\item{na.rm}{Logical. Should missing values be removed? Default \code{TRUE}.}

\item{all_na}{Character. What to do when all values are NA:
\code{"error"} (default) throws an error, \code{"base"} returns what base R does
with \code{na.rm = TRUE}, \code{"na"} returns \code{NA}.
If \code{NULL}, uses \code{getOption("tidyna.all_na", "error")}.}
}
\value{
A length-one numeric vector.
}
\description{
Drop-in replacement for \code{\link[stats:weighted.mean]{stats::weighted.mean()}} that defaults to
\code{na.rm = TRUE} and warns when missing values are removed.
Unlike base R, missing values in either \code{x} or \code{w} cause the
corresponding pair to be removed.
}
\examples{
x <- c(1, 2, NA, 4)
w <- c(1, 1, 1, 1)
weighted.mean(x, w)

}
